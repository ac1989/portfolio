{
  "scripts": {
    "build": "parcel build index.html --public-url . -d ./docs",
    "clean": "rm -rf .cache rm -rf dist"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "node-sass": "^4.9.0"
  },
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "lodash": "^4.17.10"
  }
}
